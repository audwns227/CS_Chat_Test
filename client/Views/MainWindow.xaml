<Window x:Class="client.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:client"
        mc:Ignorable="d"
        Title="Chatting System" Height="500" Width="500" Background="LightGray">

    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="6*" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Border Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="3" Background="White" Padding="10" Margin=" 10">
            <ListBox ItemsSource="{Binding messageList}" >
                <b:Interaction.Behaviors>
                    <local:AutoScrollBehavior />
                </b:Interaction.Behaviors>
            </ListBox>
        </Border>

        <TextBox 
            Grid.Row="1" 
            Grid.Column="0" 
            Grid.ColumnSpan="2" 
            Text="{Binding textModel.Text, UpdateSourceTrigger=PropertyChanged}"
            Background="White" 
            Padding="10" 
            Margin="10" />

        <Button 
            Grid.Row="1" 
            Grid.Column="2" 
            Content="Send" 
            FontWeight="Bold" 
            Background="White"
            Foreground="Black"
            Cursor="Hand"
            Margin="10" 
            Command="{Binding SendCommand}"/>

    </Grid>
</Window>
